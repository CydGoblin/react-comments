import {FC} from "react";

export const CommentActions: FC = () => {
  return (
    <div>
      <button>reply</button>
      <button>delete</button>
      <button>edit</button>
    </div>
  );
};

export default CommentActions